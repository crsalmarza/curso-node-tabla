# Notas
Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -h, --hasta    indica cuantos valores de la tabla se deben calcular por
                 defecto 10                               [number] [default: 10]
  -l, --listar   Muestra la tabla en pantalla         [boolean] [default: false]
```